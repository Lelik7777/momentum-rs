import {DATE, ENG, FLICKR, GH, GREETING, PLAYER, QUOTE, RU, TIME, WEATHER} from "./config";

export const state = {
    lang: [RU, ENG],
    images: [GH, FLICKR],
    blocks: [TIME, DATE, GREETING, WEATHER, QUOTE, PLAYER]
}